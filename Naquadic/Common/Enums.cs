namespace Naquadic.Common.Enums;

public enum PanMode
{
    Balance = 0,
    Pan,
}

public enum AttenuationModel
{
    None,
    Inverse,
    Linear,
    Exponential,
}

public enum Positioning
{
    Absolute,
    Relative,
}
